services:
  nest-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: leads-register-api
    restart: always
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    env_file:
      - .env
